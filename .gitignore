# Ferramentas de agente (skills do Claude Code) — locais, não vão para o repositório
.agents/
.claude/
skills-lock.json

# Fotos enviadas pelos usuários — dados da loja, não fazem parte do código
ListasCompras/wwwroot/uploads/

# Material-fonte de logo/design — o resultado final usado no projeto (ex: icone.ico
# do PingoInstaller) fica versionado; os arquivos originais/brutos, não.
docs/logos/
docs/screenshots/

# Log de mudanças e roteiro — ferramentas de acompanhamento interno, não fazem
# parte do que o repositório expõe publicamente.
CHANGES.md
ROADMAP.md
ROTEIRO_TESTE.md
anotacao.md
